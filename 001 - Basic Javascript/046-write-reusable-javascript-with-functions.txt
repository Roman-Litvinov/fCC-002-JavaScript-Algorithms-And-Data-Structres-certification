// define a function (no taken parameters)
// (all code between curly braces gets executed on function call):
function reusableFunction() {
  console.log("Hi World");
}

// call, or INVOKE, a function:
reusableFunction();

