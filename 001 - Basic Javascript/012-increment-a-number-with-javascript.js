/* "To increment a variable" means "to add 1 to the variable".
  Incrementation is done by using the operator ++, so instead 
  of doing:
    myVar = myVar + 1;
  we do:
    myVar++;
  which is.. shorter?, I guess.
*/

var myVar = 87;

// Only change code below this line

// increment the myVar variable:
myVar++;

console.log("myVar is now incremented and is equal to " + myVar + ".");
