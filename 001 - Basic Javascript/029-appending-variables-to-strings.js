// Change code below this line

var someAdjective;
someAdjective = "exciting and interesting and breathtaking";

// append a string literal to a string variable
// using the += operator:

someAdjective += " sometimes, when everything comes together well!";
// console.log(someAdjective);


var myStr = "Learning to code is ";

// and now append a VARIABLE to a string variable
// using the += operator:
myStr += someAdjective;
console.log(myStr);
