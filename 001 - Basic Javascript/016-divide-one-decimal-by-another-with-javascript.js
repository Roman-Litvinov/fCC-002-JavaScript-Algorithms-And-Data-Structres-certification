/* You can divide one float by another using the same
  division operator / as with regular numbers, like so: */

var quotient = 4.4 / 2.0;


console.log("4.4 divided by 2.0 equals " + quotient + ".");
