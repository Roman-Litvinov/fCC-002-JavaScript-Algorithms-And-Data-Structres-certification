
var lastName = "Lovelace";

// get the last character in the string by using its index,
// which equals its length minus 1 (because indices start with 0):

var lastLetterOfLastName = lastName[lastName.length-1];
console.log(lastName.length);
console.log("The last character in the last name is "+lastLetterOfLastName+".");