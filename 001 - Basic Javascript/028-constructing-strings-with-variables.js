// Only change code below this line

// You can use variables to insert their values into a string,
// using the concatenation + operator, like so:

var myName = "Roma";
var myStr = "My name is " + myName + " and I am well!";
console.log (myStr);