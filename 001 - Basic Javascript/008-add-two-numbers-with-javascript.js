// Number addition operations are done using the addition operator +, like so:

var sum = 10 + 10;

// NOTE: sum is declared and initialized in the same statement,
// the addition is performed first, and then the result is assigned
// to the variable sum as its initialization value.