// Change array elements using indexes to address them:

// set up an array:
var myArray = [18,64,99];

// change its 1st element's value to 45:
myArray[0] = 45;

console.log("The 1st element of the", myArray, "array is", myArray[0]+".");


// SIDE NOTE - when console.log() is given arguments separated by commas,
// it inserts spaces between outputted values, no matter whether they are
// strings or numbers or whatever):
console.log("45","12",36);
