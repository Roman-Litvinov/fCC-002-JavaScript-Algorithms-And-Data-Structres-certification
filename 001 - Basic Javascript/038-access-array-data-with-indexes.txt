// access array data using the index of the required array element,
// like so (NOTE - indices start with 0, so the index of the 1st
// array element is 0, not 1!):

// set up an array with 3 elements with indices 0, 1, and 2:
var myArray = [50,60,70];

// declare a new var and init it with the 1st array element (of index 0):
var myData = myArray[0];

console.log("The 1st element of the array is " + myData + ".");
